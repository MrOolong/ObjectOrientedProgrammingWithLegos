- Object orientation is still programming but it puts the focus on the item being built instead of the workers doing the work.
- A class is a blueprint. A class can contain methods that allow objects to do things. A class is a really advanced instruction manual. Classes are unique.
- An object is an instance of the blueprint. Objects are unlimited.